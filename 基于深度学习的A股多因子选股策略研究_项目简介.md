# 基于深度学习的A股多因子选股策略研究(AShare-DeepAlpha: Deep Learning for A-Share Multi-Factor Stock Selection)

## 一、项目概述

本项目旨在构建一个完整的端到端量化策略研发流程，探索深度学习模型在A股市场量价多因子选股中的应用。项目以历史日频行情数据为基础，通过特征工程构建多维因子体系，利用深度学习模型（如LSTM、Transformer）预测股票未来收益，并基于预测信号构建投资组合进行回测，最终以扣除交易成本后的夏普比率、最大回撤等指标客观评估策略表现。

本项目不追求“预测准确率”这一单一指标，而是强调**组合层面的绩效评估**、**严格的时序防泄露处理**与**真实交易约束模拟**，力求在金融时间序列低信噪比的条件下，验证深度学习模型相对于传统线性模型和树模型的增量价值。

项目研究结论以样本外表现为准，不预设深度学习模型一定有效。所有模型均需与简单因子组合、线性模型等基准进行比较，并在扣除交易成本后评估其稳定性。

## 二、项目目标

1. **流程完整性**：独立走通“数据获取 → 特征工程 → 模型训练 → 策略回测 → 绩效评估”全流程，形成可复现的研究框架。
2. **金融数据适配性**：针对金融时序数据的非平稳性、低信噪比和时序依赖特征，设计合理的模型架构与训练策略（如时序交叉验证、早停机制）。
3. **绩效评估客观性**：以组合层面的年化收益率、夏普比率、最大回撤、卡玛比率、换手率和交易成本后收益等指标为核心评价标准，并与匹配股票池的等权组合和宽基指数进行对比分析。
4. **增量价值验证**：建立传统因子组合、线性模型和树模型基线，检验深度学习模型是否带来稳定的样本外增量。
5. **结果可解释性**：通过因子IC分析、特征消融、滚动特征重要性和风险暴露分析等手段，理解模型决策逻辑，避免仅依据单次回测结果下结论。

## 三、项目架构

项目采用模块化设计，各模块职责清晰、耦合度低，便于迭代与复现。

### 3.1 数据层

- **数据源**：A股日频行情数据（建议优先选取沪深300历史成分股或动态全A股股票池，时间范围建议覆盖2018年至项目运行时的最新完整交易日）。
- **数据版本**：保存原始数据、复权数据、数据下载时间、接口版本和股票池快照，保证研究结果可复现。不同数据源不得在未校验字段定义和复权方式的情况下直接混用。
- **股票池构建**：使用每个交易日当时可获得的成分股名单和股票状态，避免使用当前成分股名单回填历史而产生幸存者偏差。
- **数据清洗**：区分停牌、涨跌停、缺失和未上市状态，谨慎处理缺失值；记录上市日期、退市日期、ST状态及交易状态，剔除上市不足一年的标的时按交易日动态计算。
- **价格与收益**：特征计算可使用复权价格，但组合收益和交易模拟需保留原始价格、分红送转等公司行为信息，并明确收益计算口径。
- **数据划分**：严格按时间顺序划分训练集、验证集、测试集和最终样本外区间，采用滚动训练、purged split和embargo机制，避免未来信息泄露。

### 3.2 特征层

- **因子构建**：从量价数据中提取动量类、波动率类、成交量类、价格形态类等因子。
- **异常值处理**：在每个交易日进行合理的去极值和截面标准化，明确处理规则，并避免使用未来交易日统计量。
- **标签构建**：明确预测周期，例如未来5日或20日收益；优先使用未来超额收益或截面排名作为标签，并明确信号生成和交易执行时点。
- **因子评估**：计算因子与未来收益的IC（信息系数）、ICIR（IC信息比率）及其滚动稳定性，因子筛选只能使用训练集和验证集，不能使用最终测试集。

### 3.3 模型层

- **基准模型**：先构建简单因子组合、Ridge/Elastic Net等线性模型，并可加入LightGBM等树模型，作为深度学习模型的对照基线。
- **模型选择**：在基准模型稳定后，以LSTM或Transformer Encoder为核心架构，输入为过去N个交易日的多因子序列，输出为未来收益率或未来超额收益的预测值。
- **训练策略**：采用滚动时序交叉验证、早停机制、学习率调度、Dropout正则化和固定随机种子；超参数调优只能使用训练集和验证集。
- **损失函数**：初期可使用MSE或Huber Loss；如使用IC或排序损失，应按交易日计算截面相关性或排名关系，避免将不同交易日样本直接混合计算。
- **模型比较**：重点比较不同模型在样本外、交易成本后组合表现上的差异，而不是仅比较训练集损失或预测准确率。

### 3.4 回测层

- **信号生成**：每个调仓日根据模型打分选取Top-K股票，等权或市值加权构建组合；第一阶段建议使用周度或月度调仓，并明确持有周期。
- **交易时点**：使用交易日t收盘前可获得的信息生成信号，在交易日t+1开盘或收盘执行，禁止使用当日收盘后才能获得的信息进行当日成交。
- **交易约束**：模拟T+1、停牌、涨跌停无法成交、退市和新股限制等A股交易约束，明确无法成交订单的处理方式。
- **交易成本**：设置手续费、印花税、滑点和市场冲击等成本参数，并对不同成本假设进行敏感性分析。
- **组合约束**：设置单只股票仓位上限、组合换手率、流动性和成交金额约束；根据研究目标选择是否进行行业、市值和Beta中性化。
- **基准对比**：使用与股票池匹配的等权组合和宽基指数，并可将沪深300指数作为额外参考基准。

### 3.5 评估层

- **绩效指标**：年化收益率、夏普比率、最大回撤、卡玛比率、信息比率、RankIC、ICIR、胜率、换手率和交易成本后收益。
- **稳健性分析**：按年份、市场环境、调仓频率、Top-K数量和交易成本进行分组及敏感性分析，报告滚动样本外表现。
- **统计检验**：使用分块自助法或其他适当方法估计收益和风险指标的不确定性，避免仅依据单次回测的夏普比率判断策略有效性。
- **可视化**：净值曲线、回撤曲线、因子IC时间序列图、滚动绩效图、特征消融结果和特征重要性排序。
- **归因分析**：分析策略收益来源，识别行业、市值、Beta、动量及流动性等主要风险暴露。

## 四、可用工具与技术栈

| 类别     | 推荐工具                         | 说明                                 |
| :------- | :------------------------------- | :----------------------------------- |
| 编程语言 | Python 3.10+                     | 量化研究与深度学习的主流语言         |
| 数据处理 | Pandas、NumPy                    | 数据清洗、因子计算、截面标准化       |
| 数据源   | Ashare、TickFlow、Tushare        | 用于原型或研究数据接入，需确认权限、字段定义并缓存数据版本 |
| 深度学习 | PyTorch 或 TensorFlow            | 构建LSTM/Transformer模型             |
| 回测框架 | Backtesting.py、Backtrader       | 轻量级回测，支持自定义策略与绩效分析 |
| 可视化   | Matplotlib、Plotly               | 净值曲线、回撤曲线、因子IC图         |
| 版本管理 | Git、GitHub                      | 代码托管、项目展示、复现文档         |
| 实验管理 | Weights & Biases、MLflow（可选） | 超参数跟踪、模型版本管理和实验复现   |
| 环境管理 | Conda、Poetry或requirements.txt   | 固定Python版本、依赖版本和随机种子   |

## 五、最小可行研究方案

为控制研究范围和过拟合风险，项目第一阶段建议采用以下配置：

1. **股票池**：沪深300历史成分股或动态全A股股票池，使用点时股票状态信息。
2. **数据频率**：日频行情数据，暂不引入分钟数据和另类数据。
3. **特征集合**：构建约20至40个量价因子，并完成去极值、标准化和缺失处理。
4. **预测目标**：预测未来5日或20日超额收益，并以截面排名作为主要信号。
5. **模型顺序**：简单因子组合 → 线性模型 → 树模型 → MLP/LSTM，Transformer作为扩展实验。
6. **调仓方式**：周度或月度调仓，选取Top-K股票等权持有。
7. **验证方式**：采用滚动样本外测试，最终测试区间只使用一次，不参与模型选择。
8. **结论标准**：以交易成本后、样本外、跨市场阶段稳定的组合表现判断模型是否具有增量价值。

## 六、项目交付物

1. **代码仓库**：包含数据获取、特征工程、模型训练、回测评估的完整脚本，结构清晰，注释充分。
2. **数据说明**：数据字典、数据来源、数据版本、股票池构建规则、复权方式和缺失值处理说明。
3. **README文档**：项目概述、环境配置、复现步骤、实验配置、结果截图和局限性说明。
4. **绩效报告**：策略净值曲线与基准对比图、关键指标表格、因子IC分析图、交易成本敏感性分析和样本外结果。
5. **研究总结**：对模型有效性、基准比较、策略风险暴露、统计不确定性和改进方向的简要讨论。

## 七、项目局限性与后续方向

- **局限性**：日频量价数据的信息量有限；滑点与市场冲击难以完全精确建模；样本期和股票池可能限制策略容量；模型对极端行情的泛化能力有待验证；免费或非标准化数据接口可能影响长期复现。
- **实盘边界**：本项目以研究和模拟回测为主，不直接等同于可实盘运行的交易系统；实盘前仍需补充实时数据、订单管理、风控、容量评估和合规审查。
- **后续方向**：在点时数据可获得的前提下引入价值、质量、成长等基本面因子；引入另类数据（舆情、财报文本）；尝试集成学习或多模型融合；探索强化学习在动态仓位管理中的应用；进行更长时间和更严格的样本外滚动测试。

------

*本项目为深度学习与量化金融的交叉实践，旨在通过严谨的工程流程与客观的绩效评估，探索机器学习在A股市场中的实际应用价值。*
